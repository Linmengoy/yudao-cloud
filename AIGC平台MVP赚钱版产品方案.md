# AIGC 平台 MVP 赚钱版产品方案

## 1. 版本定位

### 1.1 版本名称

AIGC 平台 MVP 赚钱版

### 1.2 核心目标

第一阶段不追求完整的 AIGC 多模态创作社区，而是优先上线最能变现的核心能力：

- 图片生成
- 视频生成
- 模型管理
- 任务队列
- 用户计费
- 资产存储
- 基础审核
- 管理后台

### 1.3 产品策略

先做一个可以收费的 AI 图片/视频生成平台，快速验证市场和模型成本，再逐步扩展脚本、配音、BGM、工作流、社区、模板市场。

本阶段的核心判断标准不是功能是否完整，而是：

- 用户能不能付费生成内容。
- 平台能不能稳定调用模型。
- 任务失败能不能自动退款。
- 管理端能不能配置模型价格和渠道商。
- 平台能不能统计收入、成本和毛利。

## 2. MVP 范围

### 2.1 本阶段必须做

| 模块 | 必做能力 | 目标 |
| --- | --- | --- |
| 图片生成 | 文生图、图生图、图片结果管理 | 快速提供低成本创作能力 |
| 视频生成 | 文生视频、图生视频、视频结果管理 | 提供高客单价核心卖点 |
| 模型管理 | 渠道商、模型、模型能力、价格、参数 | 支持多模型接入和运营调价 |
| 任务系统 | 创建任务、排队、执行、回调、重试、失败 | 保证异步生成稳定运行 |
| 计费系统 | 钱包、积分、预冻结、扣费、退款、流水 | 支持上线收费 |
| 资产中心 | 图片、视频、缩略图、生成来源、审核状态 | 管理生成结果 |
| 基础审核 | 输入文本审核、生成结果审核状态 | 降低内容风险 |
| 管理后台 | 模型配置、任务监控、用户消耗、计费记录 | 支持运营管理 |

### 2.2 本阶段暂不做

| 暂缓模块 | 暂缓原因 |
| --- | --- |
| 剧本生成 | 可后续作为创作增强能力接入 |
| 分镜管理 | 依赖脚本和工作流，第一阶段可不做 |
| 配音 / BGM | 不是首批变现最短路径 |
| 时间线编辑 | 开发成本较高，可先做单任务生成 |
| 成片合成 | 可先让用户下载单个视频结果 |
| 工作流编排 | 可先做固定图片/视频生成流程 |
| 社区 | 先验证付费生成，再做传播增长 |
| 模板市场 | 依赖社区和模板沉淀 |
| 创作者激励 | 依赖社区和交易体系 |
| 复杂推荐 | 社区阶段再建设 |

## 3. MVP 产品闭环

```text
用户注册 / 登录
  ↓
充值积分 / 获得免费额度
  ↓
选择生成类型：图片 / 视频
  ↓
选择模型和参数
  ↓
输入提示词 / 上传参考图
  ↓
系统计算预计消耗
  ↓
提交任务并冻结积分
  ↓
任务进入队列
  ↓
调用模型渠道商
  ↓
获取生成结果
  ↓
内容审核与资产入库
  ↓
任务成功扣费 / 失败退款
  ↓
用户查看、下载、再次生成
```

## 4. 核心功能设计

## 4.1 图片生成

### 4.1.1 支持能力

- 文生图
- 图生图
- 图片尺寸选择
- 图片比例选择
- 生成数量选择
- 风格选择
- 负向提示词
- 种子值
- 参考图上传
- 生成历史
- 图片下载
- 图片重新生成
- 图片收藏

### 4.1.2 推荐首批参数

| 参数 | 说明 |
| --- | --- |
| prompt | 正向提示词 |
| negativePrompt | 负向提示词 |
| modelId | 模型 ID |
| size | 图片尺寸 |
| ratio | 图片比例 |
| batchSize | 生成数量 |
| style | 风格 |
| seed | 随机种子 |
| referenceImage | 参考图 |

### 4.1.3 图片生成任务状态

```text
待冻结 → 待排队 → 生成中 → 下载中 → 审核中 → 成功
                                  ↓
                                失败 → 退款
```

### 4.1.4 图片生成资产

每张图片生成成功后，需要生成一条资产记录，记录：

- 图片地址
- 缩略图地址
- 图片宽高
- 文件大小
- 来源任务
- 使用模型
- 提示词
- 用户 ID
- 审核状态
- 下载次数

## 4.2 视频生成

### 4.2.1 支持能力

- 文生视频
- 图生视频
- 首帧图上传
- 视频比例选择
- 视频时长选择
- 视频清晰度选择
- 运镜描述
- 风格选择
- 生成历史
- 视频预览
- 视频下载
- 视频重新生成

### 4.2.2 推荐首批参数

| 参数 | 说明 |
| --- | --- |
| prompt | 视频提示词 |
| modelId | 模型 ID |
| generationMode | 文生视频 / 图生视频 |
| inputImage | 输入图片 |
| ratio | 视频比例 |
| duration | 视频时长 |
| resolution | 分辨率 |
| cameraMovement | 运镜 |
| style | 风格 |
| seed | 随机种子 |

### 4.2.3 视频生成任务状态

```text
待冻结 → 待排队 → 生成中 → 回调中 → 下载中 → 审核中 → 成功
                                            ↓
                                          失败 → 退款
```

### 4.2.4 视频生成资产

每个视频生成成功后，需要生成一条资产记录，记录：

- 视频地址
- 封面地址
- 视频时长
- 分辨率
- 文件大小
- 来源任务
- 使用模型
- 提示词
- 用户 ID
- 审核状态
- 下载次数

## 5. 模型管理

### 5.1 模型管理目标

模型管理是 MVP 能否赚钱的核心。必须支持运营人员在后台配置：

- 接哪个渠道商
- 哪个模型上线
- 哪个模型下线
- 每个模型怎么定价
- 每个模型支持哪些能力
- 每个模型允许用户传哪些参数
- 某个渠道异常时如何处理

### 5.2 渠道商管理

核心字段：

| 字段 | 说明 |
| --- | --- |
| providerCode | 渠道商编码 |
| providerName | 渠道商名称 |
| apiBaseUrl | API 地址 |
| authType | 鉴权方式 |
| apiKey | API Key，需加密存储 |
| status | 启用 / 禁用 |
| timeoutSeconds | 超时时间 |
| rateLimit | 限流配置 |
| balance | 渠道余额 |
| healthStatus | 健康状态 |
| remark | 备注 |

### 5.3 模型配置

核心字段：

| 字段 | 说明 |
| --- | --- |
| modelCode | 模型编码 |
| modelName | 模型名称 |
| providerId | 渠道商 ID |
| modelType | 图片 / 视频 |
| capability | 文生图 / 图生图 / 文生视频 / 图生视频 |
| status | 启用 / 禁用 |
| sort | 展示排序 |
| publicVisible | 用户端是否展示 |
| defaultModel | 是否默认模型 |
| costPrice | 平台成本价 |
| salePrice | 用户销售价 |
| billingUnit | 计费单位 |
| maxConcurrent | 最大并发 |
| timeoutSeconds | 超时时间 |

### 5.4 模型参数模板

不同模型参数不同，必须配置化，避免写死。

示例：

| 参数 | 类型 | 是否必填 | 默认值 | 可选值 |
| --- | --- | --- | --- | --- |
| ratio | select | 是 | 16:9 | 1:1、9:16、16:9 |
| duration | select | 是 | 5 | 5、10 |
| resolution | select | 是 | 720p | 720p、1080p |
| batchSize | number | 否 | 1 | 1-4 |
| seed | number | 否 | 空 | 任意整数 |

### 5.5 模型路由策略

MVP 阶段可以先做简单路由：

- 用户选择指定模型时，使用用户选择的模型。
- 用户不选择模型时，使用默认模型。
- 模型禁用时，不允许提交任务。
- 渠道商禁用时，不允许提交任务。
- 模型失败时，允许后台手动重跑。

后续增强：

- 按成本最低路由。
- 按成功率最高路由。
- 按用户等级路由。
- 按队列压力路由。
- 自动切换备用模型。

## 6. 任务系统

### 6.1 任务类型

MVP 阶段任务类型建议只保留：

- IMAGE_TEXT_TO_IMAGE：文生图
- IMAGE_TO_IMAGE：图生图
- VIDEO_TEXT_TO_VIDEO：文生视频
- VIDEO_IMAGE_TO_VIDEO：图生视频
- ASSET_AUDIT：资产审核

### 6.2 任务状态

| 状态 | 说明 |
| --- | --- |
| WAIT_FREEZE | 待冻结 |
| WAIT_QUEUE | 待排队 |
| QUEUING | 排队中 |
| RUNNING | 执行中 |
| CALLBACK_WAITING | 等待回调 |
| DOWNLOADING | 下载结果 |
| AUDITING | 审核中 |
| SUCCESS | 成功 |
| FAILED | 失败 |
| CANCELED | 已取消 |
| REFUNDED | 已退款 |

### 6.3 任务必须记录

- 用户 ID
- 任务类型
- 模型 ID
- 渠道商 ID
- 请求参数
- 外部任务 ID
- 任务状态
- 任务进度
- 失败原因
- 重试次数
- 冻结记录 ID
- 扣费记录 ID
- 输出资产 ID
- 创建时间
- 完成时间

### 6.4 幂等要求

必须保证：

- 同一个任务不能重复冻结积分。
- 同一个任务不能重复扣费。
- 同一个任务不能重复退款。
- 同一个渠道回调不能重复创建资产。
- 同一个任务重试不能覆盖历史结果。

## 7. 计费系统

### 7.1 计费目标

MVP 阶段计费系统要做到：

- 用户充值后获得积分。
- 生成前计算预计消耗。
- 提交任务时冻结积分。
- 任务成功后确认扣除。
- 任务失败后自动退回。
- 后台可以配置不同模型价格。
- 平台可以统计收入、成本和毛利。

### 7.2 账户模型

| 对象 | 说明 |
| --- | --- |
| wallet | 用户钱包，记录余额和冻结余额 |
| freeze | 冻结记录，任务提交时创建 |
| billingRecord | 计费流水，成功扣费或退款时创建 |
| costRecord | 成本记录，记录渠道实际成本 |

### 7.3 钱包字段

| 字段 | 说明 |
| --- | --- |
| userId | 用户 ID |
| balance | 可用积分 |
| frozenBalance | 冻结积分 |
| totalRecharge | 累计充值 |
| totalConsume | 累计消费 |
| totalRefund | 累计退款 |

### 7.4 计费流程

```text
用户提交任务
  ↓
根据模型和参数计算价格
  ↓
检查余额是否充足
  ↓
冻结积分
  ↓
创建生成任务
  ↓
任务成功
  ↓
确认扣费并记录流水
```

失败流程：

```text
任务失败
  ↓
释放冻结积分
  ↓
记录退款流水
  ↓
任务状态变更为已退款
```

### 7.5 定价方式

图片生成建议：

| 计费方式 | 示例 |
| --- | --- |
| 按张计费 | 1 张图消耗 2 积分 |
| 按模型计费 | 普通模型 2 积分，高级模型 8 积分 |
| 按批量计费 | 生成 4 张按 4 倍计费 |
| 按规格计费 | 高清图额外加价 |

视频生成建议：

| 计费方式 | 示例 |
| --- | --- |
| 按次计费 | 生成一次视频消耗 50 积分 |
| 按时长计费 | 每 5 秒消耗 50 积分 |
| 按分辨率计费 | 1080p 比 720p 加价 |
| 按模型计费 | 高级视频模型价格更高 |

### 7.6 商业化建议

- 新用户赠送少量免费积分。
- 视频生成价格明显高于图片生成。
- 高级模型仅会员或高余额用户可用。
- 失败任务自动退款，降低用户投诉。
- 支持充值套餐，例如 9.9、29.9、99、299。
- 后台可随时调整模型售价。
- 记录每次任务成本，计算毛利。

## 8. 资产中心

### 8.1 MVP 资产范围

- 图片资产
- 视频资产
- 缩略图 / 封面
- 用户上传参考图

### 8.2 资产字段

| 字段 | 说明 |
| --- | --- |
| id | 资产 ID |
| userId | 用户 ID |
| taskId | 来源任务 ID |
| assetType | 图片 / 视频 |
| fileUrl | 文件地址 |
| thumbnailUrl | 缩略图 |
| fileSize | 文件大小 |
| width | 宽度 |
| height | 高度 |
| duration | 视频时长 |
| modelId | 模型 ID |
| prompt | 提示词 |
| auditStatus | 审核状态 |
| visibility | 私有 / 公开 |
| downloadCount | 下载次数 |

### 8.3 资产能力

- 查看生成历史
- 预览图片 / 视频
- 下载图片 / 视频
- 删除资产
- 重新生成
- 复制提示词

## 9. 基础审核

### 9.1 MVP 审核策略

第一阶段不需要做复杂风控，但必须保留审核状态和扩展点。

建议实现：

- 提示词敏感词检测。
- 用户上传图片基础审核。
- 生成结果记录审核状态。
- 后台支持人工标记违规。
- 违规资产禁止下载或展示。

### 9.2 审核状态

| 状态 | 说明 |
| --- | --- |
| PENDING | 待审核 |
| PASS | 通过 |
| REJECT | 拒绝 |
| MANUAL_REVIEW | 人工复审 |

## 10. 管理后台

### 10.1 必做菜单

| 菜单 | 功能 |
| --- | --- |
| 渠道商管理 | 配置 API 地址、Key、状态、余额、限流 |
| 模型管理 | 配置图片/视频模型、能力、价格、参数、上下线 |
| 任务管理 | 查看任务状态、失败原因、重试、手动退款 |
| 资产管理 | 查看生成图片/视频、审核状态、违规处理 |
| 用户钱包 | 查看余额、冻结、充值、消耗、退款 |
| 计费流水 | 查看扣费、退款、成本、毛利 |
| 价格配置 | 配置不同模型、规格、时长的售价 |
| 敏感词管理 | 配置提示词敏感词 |

### 10.2 管理后台优先级

第一优先级：

- 渠道商管理
- 模型管理
- 任务管理
- 用户钱包
- 计费流水

第二优先级：

- 资产管理
- 价格配置
- 敏感词管理
- 成本统计

## 11. 推荐数据库表

### 11.1 模型与渠道

| 表名 | 说明 |
| --- | --- |
| ai_model_provider | 模型渠道商 |
| ai_model_config | 模型配置 |
| ai_model_capability | 模型能力 |
| ai_model_param_template | 模型参数模板 |
| ai_model_price | 模型价格配置 |

### 11.2 任务

| 表名 | 说明 |
| --- | --- |
| ai_task | 生成任务主表 |
| ai_task_log | 任务日志 |
| ai_task_callback | 渠道回调记录 |
| ai_task_result | 任务结果 |

### 11.3 资产

| 表名 | 说明 |
| --- | --- |
| ai_asset | 资产主表 |
| ai_media_file | 媒体文件 |

### 11.4 计费

| 表名 | 说明 |
| --- | --- |
| ai_wallet | 用户钱包 |
| ai_quota_freeze | 积分冻结记录 |
| ai_billing_record | 计费流水 |
| ai_cost_record | 成本记录 |
| ai_recharge_order | 充值订单 |

### 11.5 审核

| 表名 | 说明 |
| --- | --- |
| ai_audit_record | 审核记录 |
| ai_sensitive_word | 敏感词 |

## 12. API 优先级

### 12.1 用户端 API

第一批必须提供：

- 获取可用模型列表
- 获取模型参数模板
- 计算任务价格
- 提交图片生成任务
- 提交视频生成任务
- 查询任务详情
- 查询任务列表
- 查询资产列表
- 下载资产
- 查询钱包余额
- 查询计费流水

### 12.2 管理端 API

第一批必须提供：

- 渠道商新增 / 修改 / 启停
- 模型新增 / 修改 / 启停
- 模型价格配置
- 模型参数配置
- 查询任务列表
- 查询任务详情
- 手动重试任务
- 手动退款
- 查询用户钱包
- 查询计费流水

## 13. 上线验收标准

### 13.1 用户侧验收

- 用户可以查看图片和视频模型。
- 用户可以提交文生图任务。
- 用户可以提交图生图任务。
- 用户可以提交文生视频任务。
- 用户可以提交图生视频任务。
- 用户提交任务前可以看到预计消耗。
- 用户余额不足时不能提交任务。
- 用户任务成功后能看到生成结果。
- 用户任务失败后积分自动退回。
- 用户可以查看生成历史和下载结果。

### 13.2 管理侧验收

- 管理员可以新增渠道商。
- 管理员可以配置模型能力和价格。
- 管理员可以上下线模型。
- 管理员可以查看任务状态和失败原因。
- 管理员可以查看用户扣费和退款流水。
- 管理员可以查看每个任务的渠道成本。
- 管理员可以对失败任务手动重试或退款。

### 13.3 技术侧验收

- 任务创建、扣费、退款具备幂等能力。
- 渠道回调重复请求不会重复扣费或重复创建资产。
- 模型调用失败能正确标记失败并退款。
- 任务日志能追踪请求参数、响应结果和错误原因。
- API Key 不明文返回前端。
- 资产文件可正常访问和下载。

## 14. 第一阶段建议开发顺序

```text
1. 模型渠道商管理
2. 模型配置与价格配置
3. 用户钱包与积分冻结
4. 任务主表与任务状态机
5. 图片生成任务接入
6. 视频生成任务接入
7. 资产入库与生成历史
8. 任务成功扣费与失败退款
9. 管理后台任务监控
10. 基础敏感词审核
11. 成本统计与毛利统计
12. 用户端生成页面优化
```

## 15. MVP 后续扩展路线

### 15.1 第二阶段

- 脚本生成
- 提示词优化
- 批量生成
- 字幕生成
- 简单成片合成
- 工作流模板

### 15.2 第三阶段

- 配音生成
- BGM 生成
- 时间线
- 短剧/广告固定流程
- 图片到视频批量生成

### 15.3 第四阶段

- 社区作品发布
- 点赞收藏评论
- 一键同款
- 模板市场
- 创作者激励

## 16. MVP 总结

第一阶段要坚持一个原则：先上线赚钱，不做大而全。

最小可赚钱闭环是：

```text
模型配置 → 用户充值 → 提交图片/视频任务 → 冻结积分 → 调用模型 → 资产入库 → 成功扣费 / 失败退款 → 用户下载结果
```

只要这个闭环稳定，平台就具备商业化基础。后续再逐步叠加脚本、配音、BGM、工作流、社区和模板市场。
