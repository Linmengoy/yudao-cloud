--- #################### 注册中心 + 配置中心相关配置 ####################

spring:
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:10.66.0.2:8848}
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      discovery:
        namespace: prod
        group: DEFAULT_GROUP
        metadata:
          version: 1.0.0
      config:
        namespace: prod
        group: DEFAULT_GROUP

--- #################### 数据库相关配置 ####################

spring:
  datasource:
    druid:
      web-stat-filter:
        enabled: true
      stat-view-servlet:
        enabled: false
      filter:
        stat:
          enabled: true
          log-slow-sql: true
          slow-sql-millis: 500
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
    dynamic:
      druid:
        initial-size: 5
        min-idle: 10
        max-active: 50
        max-wait: 60000
        time-between-eviction-runs-millis: 60000
        min-evictable-idle-time-millis: 600000
        max-evictable-idle-time-millis: 1800000
        validation-query: SELECT 1 FROM DUAL
        test-while-idle: true
        test-on-borrow: false
        test-on-return: false
        pool-prepared-statements: true
        max-pool-prepared-statement-per-connection-size: 20
      primary: master
      datasource:
        master:
          url: ${INFRA_DB_MASTER_URL:jdbc:mysql://mysql:3306/infra_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true&rewriteBatchedStatements=true}
          username: ${INFRA_DB_USERNAME:root}
          password: ${INFRA_DB_PASSWORD:123456}
        slave:
          lazy: true
          url: ${INFRA_DB_SLAVE_URL:${INFRA_DB_MASTER_URL:jdbc:mysql://mysql:3306/infra_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true&rewriteBatchedStatements=true}}
          username: ${INFRA_DB_SLAVE_USERNAME:${INFRA_DB_USERNAME:root}}
          password: ${INFRA_DB_SLAVE_PASSWORD:${INFRA_DB_PASSWORD:123456}}

  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      database: ${INFRA_REDIS_DATABASE:2}
      password: ${REDIS_PASSWORD:}

--- #################### MQ 消息队列相关配置 ####################

rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:rocketmq:9876}

spring:
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

--- #################### 定时任务相关配置 ####################

xxl:
  job:
    enabled: true
    admin:
      addresses: ${XXL_JOB_ADMIN_ADDRESSES:http://xxl-job-admin:8080/xxl-job-admin}

--- #################### 服务保障相关配置 ####################

lock4j:
  acquire-timeout: 3000
  expire: 30000
