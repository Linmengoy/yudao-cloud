# Draw2Video 使用指南

Docusaurus 3 独立文档站原型，默认以 `/guide/` 子路径发布。

```bash
pnpm install
pnpm start
pnpm build
```

构建产物在 `build/`。管理端入口在 `yudao-ui/draw2video-admin/src/views/aigc/guide/index.vue`，可通过 `VITE_APP_GUIDE_URL` 覆盖嵌入地址。
